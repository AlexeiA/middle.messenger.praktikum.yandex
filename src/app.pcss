@import './variables.css';

html, body, #app, div[data-layout="1"] {
  height: 100%;
  width: 100%;
  margin: 0;
  padding: 0;
  font-family: Arial, sans-serif;
  font-size: 12pt;
}

main {
  h1, p, a {
    margin: 0.5rem;
  }
}
